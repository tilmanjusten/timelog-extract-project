# timelog-extract-project

Extract project logs from (Mac-) timelog log files.

```bash
./extract-log <project_name> [<input_filename>]
```

`project_name` is camel case. Result will be written to `project_name.txt`.